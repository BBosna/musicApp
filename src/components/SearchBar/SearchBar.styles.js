import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: '#ecf0f1',
    padding: 5,
    margin: 5,
    borderRadius: 5,
    color: 'white',
  },
});
